/** @type {import('next').NextConfig} */
const nextConfig = {
	reactStrictMode: true,
	images: {
		domains: ["drive.google.com", "image-component.nextjs.gallery", "onedrive.live.com","ams03pap001files.storage.live.com"],
	},
};

module.exports = nextConfig;
