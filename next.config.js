/** @type {import('next').NextConfig} */
const nextConfig = {
	reactStrictMode: true,
	images: {
		domains: ["drive.google.com", "image-component.nextjs.gallery"],
	},
};

module.exports = nextConfig;
